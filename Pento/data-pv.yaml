apiVersion: v1
kind: PersistentVolume
metadata:
  name: data-store
  labels: 
    app: file-server
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem 
  hostPath:
    path: /web
